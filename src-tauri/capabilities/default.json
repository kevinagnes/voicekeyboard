{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the settings window",
  "windows": ["settings", "recording-overlay"],
  "permissions": ["core:default"]
}
